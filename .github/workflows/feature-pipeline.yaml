name: feature-pipeline

on:
  push:
    branches: [ main ]

jobs:
  feature-caller:
    uses: github.com/PrudhviMV/workflows/.github/workflows/feature-nodejs-pipeline.yaml
    with:
      project: roboshop
      component: catalogue
      environment: dev