name: feature-pipeline

on: [push]

jobs:
  feature-caller:
    uses: PrudhviMV/workflows/blob/main/.github/worfkflows/feature-nodejs-pipeline.yaml@main
    #uses: prudhvi-actions-test/workflows/.github/workflows/feature-nodejs-pipeline.yaml@main
    with:
      project: roboshop
      component: catalogue
      environment: dev