name: feature-pipeline

on:
  push:
    branches: [ main ]

jobs:
  feature-caller:
    uses: PrudhviMV/workflows/.github/workflows/feature-nodejs-pipeline.yaml@main
    with:
      project: roboshop
      component: catalogue
      environment: dev